#include<stdio.h>

/*a code to print the true and false values in the gcc*/
int main()
{
	printf("false value in gcc is: %d \n", 5==1);  /*using relational operator "of equal" */
	printf("true value in gcc is: %d", 5!=1);   /*using relational operator "not equal" */
	
}